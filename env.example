# Copy to a local .env — never commit secrets.
# Weave is optional. The binder is valid on fixtures without these.

WANDB_API_KEY=
WANDB_ENTITY=superhero
WANDB_PROJECT=tracelaw


# Live GPU robot: molab notebook menu → "Pair with an agent" gives both.
MOLAB_URL=
MARIMO_TOKEN=

# TypeSafe AI second judge (console.typesafe.ai → API key).
TYPESAFE_API_KEY=
